import localeText from './localeText'
import localeTextArea from './localeTextArea'
import localePortableText from './localePortableText'
import caseSchema from './case'

export const schemaTypes = [localeText, localeTextArea, localePortableText, caseSchema]
